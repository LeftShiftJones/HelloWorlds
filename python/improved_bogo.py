import random

def main():
    list = input("Please input a set of random numbers: ").split()


def bogo_sort(list):
    for i in range(0, len(list)-1):
        if list[i] > list

def swap(i, j, list):
    temp = list[j]
    list[j] = list[i]
    list[i] = temp

main()
